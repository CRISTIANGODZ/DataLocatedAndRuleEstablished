<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cqupt.case_data_location.mapper.ThemesMapper">
    <!--
    
  -->


    <!--自定义映射-->

    <!--自定义映射-->


    <!--下面撰写sql语句-->

    <!--void addTheme(Themes themes);-->
    <insert id="addTheme" useGeneratedKeys="true" keyProperty="themeId">
        insert into themes (theme_name)
        values (#{themeName})
    </insert>

    <!--void deleteThemes(@Param("themeId") Long themeId);-->
    <delete id="deleteThemes">
        DELETE FROM themes
        WHERE theme_id = #{themeId}
    </delete>

    <!--上面撰写sql语句-->

</mapper>