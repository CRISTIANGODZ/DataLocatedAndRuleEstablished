<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cqupt.electroniccase.mapper.TextsMapper">
    <!--
    
  -->


    <!--自定义映射-->

    <!--自定义映射-->


    <!--下面撰写sql语句-->

    <!--void addText(Texts texts);-->
    <insert id="addText" useGeneratedKeys="true" keyProperty="textId">
        insert into texts (patient_id, first_category_id, disease_id, title, content, updatetime, theme_id)
        values (#{patientId}, #{firstCategoryId}, #{diseaseId}, #{title}, #{content}, NOW(), #{themeId})
    </insert>

    <!--上面撰写sql语句-->

</mapper>